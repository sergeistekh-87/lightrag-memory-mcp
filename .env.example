# LightRAG server URL
LIGHTRAG_BASE_URL=http://localhost:9621

# API key (set in LightRAG .env as LIGHTRAG_API_KEY)
LIGHTRAG_API_KEY=your-secret-key-here

# Default timeout for all requests in seconds (default: 60)
# Increase for slow networks or large documents
LIGHTRAG_TIMEOUT=60
